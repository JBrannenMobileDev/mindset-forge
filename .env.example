# Server-side secrets — set via `firebase functions:secrets:set`, never commit real values.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Shared secret for the RevenueCat webhook Authorization header (e.g. `openssl rand -hex 32`).
REVENUECAT_WEBHOOK_SECRET=your_revenuecat_webhook_secret_here
